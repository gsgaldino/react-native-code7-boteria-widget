import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  carouselCardInfoText: {
    color: '#5a5d68',
    fontSize: 16,
  },
  carouselCardInfoTitle: {
    fontWeight: 'bold',
    marginBottom: 4,
  },
});
