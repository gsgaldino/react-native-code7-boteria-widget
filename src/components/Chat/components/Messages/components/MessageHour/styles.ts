import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingBottom: 8,
    paddingTop: 4,
  },
  text: {
    fontSize: 12,
  },
  leftAligned: {
    textAlign: 'right',
  },
  rightAligned: {
    textAlign: 'right',
  },
});
